name: Publish to npm

on:
  push:
    branches: [ main, master ]
    tags:
      - 'v*.*.*'
  pull_request:
    branches: [ main, master ]

jobs:
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run lint
        run: npm run lint
      
      - name: Run type check
        run: npm run typecheck
      
      - name: Run tests
        run: npm test
      
      - name: Build project
        run: npm run build
      
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: dist/
          retention-days: 3

  publish:
    name: Publish to npm
    runs-on: ubuntu-latest
    needs: build-and-test
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master' || startsWith(github.ref, 'refs/tags/v'))
    timeout-minutes: 10
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'
          registry-url: https://registry.npmjs.org/
      
      - name: Install dependencies
        run: npm ci
      
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-artifacts
          path: dist/
      
      - name: Determine version strategy
        id: version-strategy
        run: |
          if [[ $GITHUB_REF =~ ^refs/tags/v ]]; then
            echo "strategy=tag" >> $GITHUB_OUTPUT
            echo "version=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
          else
            echo "strategy=auto" >> $GITHUB_OUTPUT
          fi
      
      - name: Update package version (auto-increment)
        if: steps.version-strategy.outputs.strategy == 'auto'
        run: |
          # Get current version from package.json
          CURRENT_VERSION=$(node -p "require('./package.json').version")
          
          # Increment patch version
          NEW_VERSION=$(npm version patch --no-git-tag-version)
          
          echo "Updated version from $CURRENT_VERSION to $NEW_VERSION"
          echo "version=$NEW_VERSION" >> $GITHUB_OUTPUT
        id: auto-version
      
      - name: Set final version
        id: final-version
        run: |
          if [[ ${{ steps.version-strategy.outputs.strategy }} == 'tag' ]]; then
            echo "version=${{ steps.version-strategy.outputs.version }}" >> $GITHUB_OUTPUT
          else
            echo "version=${{ steps.auto-version.outputs.version }}" >> $GITHUB_OUTPUT
          fi
      
      - name: Publish to npm
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
      
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/v')
        with:
          name: Release ${{ steps.final-version.outputs.version }}
          body: |
            ## Release ${{ steps.final-version.outputs.version }}
            
            ### Changes
            - Automated release from GitHub Actions
            - Built with Node.js 20.x
            - Version: ${{ steps.final-version.outputs.version }}
          files: |
            dist/**/*
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Create release log
        run: |
          cat > RELEASE_LOG.md << EOF
          # Release Log - ${{ steps.final-version.outputs.version }}
          
          ## Release Details
          - **Version**: ${{ steps.final-version.outputs.version }}
          - **Date**: $(date -u +"%Y-%m-%d %H:%M:%S UTC")
          - **Commit**: ${{ github.sha }}
          - **Branch**: ${{ github.ref_name }}
          - **Trigger**: ${{ github.event_name }}
          
          ## Build Information
          - **Node.js Version**: 20.x
          - **npm Version**: $(npm --version)
          - **Build Status**: Success
          
          ## Published Packages
          - **Package Name**: ${{ github.event.repository.name }}
          - **npm Registry**: https://www.npmjs.com/package/${{ github.event.repository.name }}
          
          ## Release Notes
          Automated release from GitHub Actions workflow.
          
          EOF
      
      - name: Upload release log
        uses: actions/upload-artifact@v4
        with:
          name: release-log
          path: RELEASE_LOG.md
          retention-days: 14
      
      - name: Send notification on success
        if: success()
        uses: 8398a7/action-slack@v3
        with:
          status: custom
          fields: workflow,job,commit,repo,ref,author,took
          custom_payload: |
            {
              "text": "🚀 ${{ github.event.repository.name }} v${{ steps.final-version.outputs.version }} has been successfully published to npm!",
              "attachments": [
                {
                  "color": "#43cc11",
                  "fields": [
                    {
                      "title": "Package",
                      "value": "${{ github.event.repository.name }}",
                      "short": true
                    },
                    {
                      "title": "Version",
                      "value": "v${{ steps.final-version.outputs.version }}",
                      "short": true
                    },
                    {
                      "title": "Repository",
                      "value": "${{ github.repository }}",
                      "short": true
                    },
                    {
                      "title": "Branch",
                      "value": "${{ github.ref_name }}",
                      "short": true
                    },
                    {
                      "title": "Commit",
                      "value": "${{ github.sha }}",
                      "short": true
                    },
                    {
                      "title": "npm Link",
                      "value": "https://www.npmjs.com/package/${{ github.event.repository.name }}",
                      "short": false
                    }
                  ]
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        continue-on-error: true
      
      - name: Send notification on failure
        if: failure()
        uses: 8398a7/action-slack@v3
        with:
          status: custom
          fields: workflow,job,commit,repo,ref,author,took
          custom_payload: |
            {
              "text": "❌ Failed to publish ${{ github.event.repository.name }} to npm!",
              "attachments": [
                {
                  "color": "#ff0000",
                  "fields": [
                    {
                      "title": "Package",
                      "value": "${{ github.event.repository.name }}",
                      "short": true
                    },
                    {
                      "title": "Branch",
                      "value": "${{ github.ref_name }}",
                      "short": true
                    },
                    {
                      "title": "Commit",
                      "value": "${{ github.sha }}",
                      "short": true
                    },
                    {
                      "title": "Workflow",
                      "value": "${{ github.workflow }}",
                      "short": true
                    },
                    {
                      "title": "Job",
                      "value": "${{ github.job }}",
                      "short": true
                    },
                    {
                      "title": "Action URL",
                      "value": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}",
                      "short": false
                    }
                  ]
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        continue-on-error: true

  cleanup:
    name: Cleanup
    runs-on: ubuntu-latest
    needs: [ build-and-test, publish ]
    if: always()
    
    steps:
      - name: Cleanup artifacts
        uses: geekyeggo/delete-artifact@v5
        with:
          name: |
            build-artifacts
            release-log
          failOnError: false
